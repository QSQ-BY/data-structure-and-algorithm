# 2026-08-30：牛客周赛 Round 159

前五题整体比较直接，这里只简单记一下仓库中留下的 B～E 四份 Python 代码，不展开逐题推导。

- B“信号”用 `lowbit` 逐个取出二进制中的 `1`，同时统计数量和最低、最高激活位。
- C“校验码”把 01 串转成整数，逐位异或后在集合中寻找只差一位的旧字符串。检查时去掉了命中后的 `break`，避免一个字符串有多个邻居时漏算。
- D“灯带”对各段长度求前缀和，再用 `bisect_left` 定位查询位置所在的段。
- E“前缀集合”用字典保存每个前缀的覆盖次数。插入和删除都要遍历到完整字符串，不能少掉最后一个前缀。

对应源码：[B](./1.小月的信号.py)、[C](./3.小月的校验码.py)、[D](./2.小月的灯带.py)、[E](./4.小月的前缀集合.py)。

本地重新运行了这四题各两组题面样例，共 8 组，输出都与题面一致。在线提交结果没有记录在这篇日志里。
