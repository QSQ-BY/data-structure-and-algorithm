# 数据结构与算法学习记录

这个仓库记录了我从零开始学习数据结构与算法的过程。

`source` 里放的是每一章的代码：一部分是基础数据结构的定义和常见操作，另一部分是配套练习。练习主要来自 LeetCode、洛谷等 OJ 网站，也有跟着课程完成的题目。`diary` 里则是我每天写完代码后的复盘，记录当天学了什么、卡在哪里，以及后来是怎么想明白的。

这里不只收集最后能够运行的代码。我更想保留学习过程中那些逐渐连起来的线索：顺序表为什么要区分容量和元素个数，链表题为什么总是在找指针关系，括号匹配如何把栈从一个结构体变成一种思考方式，递归又是怎么在二叉树里真正派上用场的。

## 目前写到哪里

截至 2026-06-19，仓库里已经有这些内容：

| 章节 | 基础内容 | 部分练习与延伸 |
| --- | --- | --- |
| 顺序表 | 创建、扩容、插入、删除、修改、释放 | 二分查找、原地移除元素、有序数组去重、滑动窗口、前缀和、有序数组平方 |
| 链表 | 单链表、循环链表、双向链表、虚拟头节点 | 反转链表、删除倒数第 n 个节点、环形链表、旋转链表、快乐数 |
| 递归函数 | 阶乘、递归出口、回溯的基本写法 | 排列、组合、子集枚举 |
| 栈与队列 | 数组栈、循环队列、链表队列 | 括号匹配、栈序列、退格字符串、表达式计算 |
| 二叉树 | 节点定义、插入、遍历、释放 | 前中后序遍历、层序遍历、线索化、广义表序列化与反序列化、哈夫曼编码、LeetCode 树题练习 |
| 堆与优先队列 | 大顶堆、小顶堆、上滤、下滤、堆顶查询、插入与弹出、堆排序、普通建堆、线性建堆、`set + pair` 模拟堆 | 重写基础结构与结构操作，从数组下标理解完全二叉树里的父子关系，比较逐个上滤建堆和从最后一个非叶子节点下滤建堆，并用小顶堆优化哈夫曼编码；目前已经开始做堆章节练习，写到数据流中的第 k 大元素、数据流中位数、合并 K 个升序链表、丑数 II、带截止时间的超市利润最大化问题、序列 m 小和，以及 HZOJ 289 生日礼物 |
| 排序算法 | 选择排序、排序正确性检查框架 | 刚开始进入排序章节，先用选择排序整理“有序区”和“待排序区”的基本思路 |

二叉树这一章已经暂时收尾。线索化和序列化各自重新手写过一遍，哈夫曼编码的代码实现也已经完成。这几天还补了二叉树相关的 LeetCode 练习，从普通层序遍历、自底向上层序遍历，写到锯齿形层序遍历和判断树的子结构。现在堆和优先队列的基础部分也已经收尾：先手写大顶堆、小顶堆、堆排序和两种建堆方式，再用小顶堆优化哈夫曼编码，最后用 `set + pair` 模拟插入、取堆顶和弹出堆顶等操作。最近的堆章节练习主要围绕动态维护展开：序列 m 小和保留每一轮最小的前 `m` 个部分和，生日礼物则把连续正负段压缩后，用集合和模拟链表不断删除或合并绝对值最小的段。排序算法这一章也已经开头，先实现了选择排序。仓库里还新建了一个 `python` 文件夹，打算在期末结束后用 Python 重写一部分算法练习。

## 目录结构

```text
.
├─ source
│  ├─ vector
│  ├─ linked list
│  ├─ recursive_function
│  ├─ stack&queue
│  ├─ binary_tree
│  ├─ heap&priority_queue
│  └─ sort_algorithm
├─ python
├─ diary
│  ├─ vector_linklist_recursive_func
│  ├─ stack_queue
│  ├─ binary_tree
│  └─ heap&priority_queue
└─ .build
```

每个章节的源码大致分成两类：

- `basic_definition&operation`：自己实现基础结构，并保留可单独运行的入口。
- `exercise`：围绕当前章节完成的练习题。

## 怎么阅读这个仓库

如果想看某个结构的实现，可以先从对应章节的基础代码开始，再看 `exercise` 里的题目。

如果想看学习过程，建议按日期翻 `diary`。日志不是教程，也没有刻意把每一天写得很顺。有时我会先跟着课程写一遍，第二天遮住代码重新实现，再把真正没懂的地方补出来。对我来说，这部分和源码同样重要。

如果不想按日期一篇篇翻，可以先从这几篇看起：

- [顺序表、链表与递归函数](diary/vector_linklist_recursive_func/2026-05-10_to_05-20.md)
- [从队列写到栈，再写到括号匹配](diary/stack_queue/2026-05-22.md)
- [表达式计算：把表达式看成一棵树](diary/stack_queue/2026-05-26.md)
- [线索化二叉树：自己写一遍才知道哪里没有懂](diary/binary_tree/2026-05-30.md)
- [从仓库选址到合并果子，二叉树这一章收尾](diary/binary_tree/2026-06-06.md)
- [开始写堆和优先队列](diary/heap&priority_queue/2026-06-07.md)
- [重写建堆，再用堆优化哈夫曼编码](diary/heap&priority_queue/2026-06-11.md)
- [用 set 和 pair 模拟堆，基础部分收尾](diary/heap&priority_queue/2026-06-12.md)
- [从第 k 大元素到数据流中位数](diary/heap&priority_queue/2026-06-13.md)
- [重写维护类题，再写合并链表和丑数](diary/heap&priority_queue/2026-06-14.md)
- [超市利润题，把贪心流程理顺](diary/heap&priority_queue/2026-06-15.md)
- [序列 m 小和，把前 m 个候选维护清楚](diary/heap&priority_queue/2026-06-17.md)
- [从生日礼物到选择排序，排序算法开头](diary/heap&priority_queue/2026-06-19.md)

## 本地运行

仓库里的代码以 C/C++ 基础练习为主，多数文件可以单独编译。部分代码使用了 `nullptr` 和 `and`，建议使用 `g++`。

## 关于更新

这个仓库会跟着学习进度继续长下去。写代码、做题、复盘，再回头重写一遍不够熟练的部分，是目前最适合我的节奏。
